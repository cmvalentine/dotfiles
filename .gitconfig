[user]
    name    = <name>
    email   = <email>
[alias]
    a       = add
    aa      = add --all
    b       = branch
    bd      = branch -d
    ca      = commit -a
    cam     = commit -am
    cl      = clone
    cm      = commit -m
    co      = checkout
    cob     = checkout -b
    com     = checkout main
    cr      = commit --amend -m # commit rename
    ds      = diff --staged
    ls      = log --pretty=format:"%C(yellow)%h%Cred%d\\ %Creset%s%Cblue\\ [%cn]" --decorate
    pl      = pull
    ph      = push
    rb      = rebase
    rec     = commit --amend --no-edit # recommit
    s       = status
    ss      = status -s
    unc     = reset --soft HEAD~1 # uncommit
    uns     = reset HEAD -- # unstage
[color]
    ui      = auto
[pull]
    rebase  = false
